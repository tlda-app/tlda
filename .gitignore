# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

.env
TODO*.md
node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# Local Yjs persistence
server/data/

# Built viewer SPA (copied from dist/ to server/public/ for unified server)
server/public/
server/public.*/
server/build-info.json

# Fleet runtime uploads / generated chat artifacts
mcp-server/fleet-data/

# Project build output
server/projects/

# Test artifacts
test-results/

# Scratch / debug scripts
scratch/
*.local.mjs

# Generated document output (rebuilt via build-svg.sh / build-html-doc.mjs)
# manifest.json is kept; everything inside doc directories is generated
public/docs/*/
!public/docs/spinoff3/

# Built assets (KaTeX fonts, etc.) — don't commit
public/assets/

# Working directories
.worktrees/
telemetry/.stack/
.claude/
.agents/
.playwright-cli/
.playwright-mcp/
.tlda-dev/
.tlda-fly/
.python-deps/
venv/
venj/
__pycache__/
*.pyc

# Personal / development artifacts
.home/
*.png
*.jpeg
*.jpg
!public/**/*.png
!public/**/*.jpg
!docs/images/*.png
console-errors.txt
TODO*.md
.dev-url
dist.broken/
dist.rescue/
docs/fleet/
bin/merge-april.sh
bin/fix-head.sh
bin/finish-merge.sh
bin/fleet-resume.sh
fleet.db
.pw.lock
.playwright/

# TypeScript incremental build artifacts
*.tsbuildinfo
.tmp/

# Build artifacts that block the live-deploy preflight.
# `runLiveDeployPreflight` treats ANY untracked file in the shared checkout as
# dirty and refuses to deploy. The packaged extension is rebuilt constantly, so
# left untracked it blocks every deploy until someone notices. Ignoring is not
# deleting: existing files stay on disk.
extensions/tlda-classroom-positron/*.vsix
/bk.html

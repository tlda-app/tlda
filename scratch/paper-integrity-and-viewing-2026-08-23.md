# Paper-text integrity and the viewing bugs — resumption point, 2026-08-23

Force-added under a gitignored directory on purpose. **This is not a report of
finished work; it is what the next person continues from.** `AGENTS.md`
§"Repository workflow" — a report can be regenerated by redoing the work, a
resumption point is the work.

Author: `paper-safety-pm` (`fleet:9111347f`), reporting to `bhief-4`.
State as of **2026-08-24 ~01:40Z**. `main` local tip `140101c7c`; deployed
`140101c7c` on `tldraw-sync-skip` (the `testing` box).

---

## STILL OPEN — start here

### 1. The freeze. Nobody has explained it.

His session on the two-root paper, 2026-08-23:

| | |
|---|---|
| 18:54–19:11Z | healthy, ~17 minutes, `live-perf` sample every ~10s |
| 22:25:28.9–22:25:29.3Z | **7 records, 0.4 seconds**, last one `sync-status: synced-remote / online`, then silence forever |

Same deployed sha for both. **So it is state, not code.** He reported the lockup
at 22:35Z; his tab is now on a different project.

**It is load-against-load, not warm-against-cold.** Filtering by *session* rather
than by time — the healthy session `mt63g2zw-fduroq` **cold-loaded the same
project at 17:41:24Z** (`navigation type: navigate`, FCP 400, domInteractive
201.5) and then ran healthily for 90 minutes to 19:11:31Z. The frozen session
`mt6dldu0-itg1n7` cold-loaded at 22:25:29Z and was dead in 0.4s. **Same code
path, same room, 4h45m apart.**

The frozen load was measurably slower before it died — domInteractive **919.6ms**
against 201.5ms, LCP 2192ms, one 1399ms longtask — **and produced no error of any
kind.** `client.log` carries warn and above; the only records are routine gesture
telemetry.

**Ruled out — do not re-derive these:**

- **Not the orphan shapes.** `docShapeCount` was 326 at 16:01Z, 341 by 17:26Z,
  343 at 22:25Z. Present on both sides of the gap, including through the healthy
  90 minutes. A 342-shape fixture therefore cannot reproduce this, because his
  own session proves 342 shapes do not suffice.
- **Not SVG cache warmth after the 22:20 build.** This was my best candidate and
  it is wrong: the output dir holds **5** cached page SVGs total, three of which
  are from my own probes tonight. The cache does not retain pages in bulk, so
  **both** loads faced on-demand rendering.
- **Not server latency.** Cold page render is ~1.0s against ~0.3s warm
  (`?_tldaCold=1`). Real, but server slowness cannot stop a browser main thread,
  and the telemetry stops dead.
- **Not asset size.** Every client-side lookup asset is modest — largest is
  `supplementary_appendix-source-map.json` at 296 KB.
- **Not a crash.** No error or exception record precedes the silence.
- **Not the profiler's business.** `client-profile.jsonl` needs a live main
  thread, so it is silent exactly when needed — last record for that document
  is 17:46Z.

**What remains in the gap, unexamined:** the builds at 22:19:49–22:20:26Z, the
settle commits, and a restore commit at 22:24:04Z — **84 seconds before he opened
it**.

**Mechanism unknown. Do not let the orphan finding become the answer** — it was
present during the 90 healthy minutes and does not cross the gap.

**Note on method for whoever continues:** filter `client.log` by `session` and by
project, never by time. Filtering by time is what made this look like
warm-against-cold for most of the night; there are also two session id spaces
(`data.sessionId` from `live-perf`, `session` from gesture telemetry), so one
browser load appears under two ids.

### 2. Skip's A/B, unanswered — no code until he answers, and bhief-4 briefs it

- **A** — fix the cleanup plainly; his 169 orphans are deleted on his next load.
- **B** — fix it forward-only; existing orphans stay.

bhief-4 recommended A to him. **Write neither version until he answers.**

---

## The orphan defect — root cause established, fix NOT written

**A cleanup that has never once fired.** `src/loaders/createShapes.ts`:

- line **83** `editor.deleteShapes(stalePages…)` — the stale-page cleanup, whose
  comment names the single-target → multi-target transition exactly
- line **97** creates every `svg-page` with `isLocked: true`
- vendored `@tldraw/editor` — `deleteShapes` runs ids through
  `_getUnlockedShapeIds`, which **filters locked ids out**

So every shape the cleanup targets is locked by the function doing the targeting.
Silent no-op, no error, no log. Present since `a93d4d1c0` (2026-05-09).

**All three `deleteShapes` in that file are dead**, because every creation site in
it locks (**97, 209, 241, 299, 322, 364**):

| call | targets | has victims? |
|---|---|---|
| **83** | `svg-page` | **yes** — 169 in his room, 3 in the fixture |
| **151** | `html-page` + `svg-figure` | **no room found in the pre-multipage format**; branch appears never entered |
| **193** | `html-page` | **yes** — 4 rooms: 21, 9, 1, 1 stale |

Line 151 is the old-format → multipage migration, which also re-parents
annotations. **It has cost nothing so far** — see the 253 below.

**The fix bhief-4 and I converged on, unwritten:** one helper this file's
cleanups go through, using `editor.run(fn, { ignoreShapeLock: true })`. Not three
wrappers — the file's invariant is "everything I create is locked", so
"everything I delete needs the override" is the matching half.

**Prior art that says this trap has been hit before:** `HtmlPageShape.tsx:694`
explicitly unlocks a shape before mutating it. Someone found locked-shape
mutation being silently ignored, fixed it inline, and did not generalise — so
three later call sites inherited it.

**Not affected:** `HtmlPageShape.tsx:242` and `:261` target generated mermaid
shapes, which we never lock (only `:1053` sets `isLocked` in that file).

### The fixture — keep it, it is the evidence

`orphan-probe` on `testing`, repo `~/worktrees/orphan-probe`. Disposable, nobody
works in it. **It also proves the fix when one lands**: reproduce with one root →
add a second in place via `PATCH /api/projects/<name>/document-roots` (in place,
so the room survives) → push a revision reaching both roots → reload. Orphans
persist across repeated loads; it does not self-heal.

---

## Annotations are intact — 253, not 47

**Corrected count.** The first pass matched `math-note` and `note`; **`note` is
not a type this app has** and `highlight` was missed entirely.

Full census across 12,997 snapshots: `geo` 124, `highlight` 63, `math-note` 47,
`understanding-line` 10, `line` 5, `draw` 4 — **253 annotation-ish shapes, all
253 parented to a live tldraw page. Zero missing, zero parented to a shape.**

**Positive control, by a different reader:** `GET /api/projects/tlda/shapes`
(server's live room reader) against the snapshot file for the same project —
exact agreement on every type (highlight 51, math-note 10, html-page 23, geo 15,
line 4). So the snapshot is not lagging the live room.

---

## Attribution — fixed and live, but know what it does not cover

**`140101c7c`.** `daemon/jsonl-ingestor.mjs:1611` called `recordEdit` only when
there was **no** edit-operation store, while `bin/fleet-daemon.mjs:490`
constructs one unconditionally — so nothing was ever recorded, and `resolveEditor`
reads only the store when the store exists, making the in-memory fallback
unreachable too. Both halves landed in `1259070ae` (2026-08-14).

The condition is **deleted**, not inverted: `recordEdit` already branches
correctly on `operation?.operation_id`.

**Live counterfactual (bhief-4 ran it across the running daemon):** before 1,561
rows / newest `10:18:38` with nothing for 10h47m → one real `Edit` → 1,562 /
newest `21:05:10`, correct `agent_id` and path.

**What it records:** `agent_id`, canonical path, and per-change
`removed_line_sha256` / `added_line_sha256` — **who removed which lines**, not
merely who touched the file.

**Trap for whoever verifies this next:** `edit_operation` is **not in the raw
JSONL**. It is synthesized at parse time by `normalizedToolInput`
(`agent-runtime/jsonl-event-extract.mjs`), using the tool_use `id` as
`operation_id`. Checking the raw file shows 0 of 725 and means nothing.

### Server-side attribution is still dead — deliberately not touched

`lastEditedBy` has **no writer**: the only one was deleted in `f6d0f9089`
(2026-08-20, "Delete parallel server source authority", 8,421 deletions), which
also deleted the test asserting the wire. `resolveEditedBy` therefore always
returns null, so `createSourceEditEvent` always returns null and **no
`source-edit` event is ever emitted**. `emitSourceEditEvent` is imported into
`server/routes/projects.mjs` and never called.

Measured: newest `lastEditedByAt` anywhere on the box was **72 hours old**
against a 10-minute window.

**Restoring it means deciding where attribution belongs on the new path — a
design conversation, not a one-liner.** `docs/what-the-old-push-did.md` lists both
this row and "the source-edit event with line regions" against the deleted
`applyAcceptedSourceEffects`; **both are now re-checked and both are gaps.**

---

## The history bugs — root causes established, nothing written

**All 736 versions of the two-root paper are unviewable**, by two stacked defects:

1. `revision/manuscript.tex` is **absent from the shadow repo** (the appendix root
   *is* present), so the primary target's historical compile cannot run. Blast
   radius: of 1,043 projects, 7 have a subdirectory main file; **only this one is
   missing it**. The subdirectory is not the cause.
2. `buildShadowPage(name, hash7, pageNum)` takes **no `texBase`** and always
   derives it from `project.mainFile`, discarding the target parsed from the URL —
   so the second root's history is unreachable even when its source is present.
   `buildCurrentPage` takes one. Confirmed live: requesting the *appendix* page
   made the server compile the *manuscript*.

**Also:** nothing anywhere has had a shadow snapshot since **2026-08-17/08-18**,
so this week's edits have no history entry at all.

**Not a finding, retired so nobody re-chases it:** `/docs/<project>/page-info.json`
404s for an `svg` project **by design** — it is the *parts* listing there, and
`refreshSvgProjectParts` documents the 404 as an intended no-op.

---

## The deletion count — what the number is and is not

Two weeks, 19 repos with `.tex`, 1,508 commits: **981** commits removed ≥40 words
of prose; **611** where it is still gone; **195** of those also shrank the file.

**It cannot be turned into "an agent deleted this" and here is why.**

- **478 of the 611 arrive as pipeline settles** — built with `commit-tree` and a
  fixed message, **no author at all**. 445 carry Skip's own git name because he
  does not type and the app commits as him.
- **68 are `Update on Overleaf.`** — the co-author editing his own paper.
- **65 are agent-authored**, 24 deletion-shaped, and the large ones announce
  themselves in the subject.

**Two distinct inflations, only one of which is filtered:**

1. churn — removed at one settle, restored at the next (filtered by the
   still-gone-today pass)
2. **bulk recording of off-git editing** — a working tree can carry weeks of
   uncommitted edits and be committed at once; every per-commit measure reads
   that as a deletion at the moment of *recording*. **Not filtered, and not
   filterable from git alone.** `3e61b9db0` is the worked example: 1,076 words
   with no successor, in a commit whose whole purpose was preserving work that
   existed only on disk. It deleted nothing.

**Dating trap:** on an Overleaf-imported commit, author and committer dates differ
by a long way — `8b53299` is authored `2025-02-04`, committed `2026-07-10`. For
"how long was this uncommitted", **`%cI` is the bound and `%aI` is not.**

Scripts, kept because the method is the reusable part, not the number:
`lost_prose.py` (sentence-survival over history) and `survives.py` (intersection
with today — note an earlier version compared each parent straight to today and
charged every *later* deletion to that commit, returning a bigger set than the
one it was filtering). Both live in this session's scratchpad under
`/private/tmp/claude-501/-Users-skip-work-tlda/1fb42e2e-.../scratchpad/`, which
**does not survive a reboot** — re-derive from the description above rather than
counting on them.

---

## Standing constraints on this work

- **Never name his papers to anyone but bhief-4.** `AGENTS.md` is explicit and
  the consequence is stated there.
- **Report to bhief-4, not to Skip.** Everything to him goes through them.
- **Do not sweep shapes from any room he works in.** What happens to the 169 is
  his decision, pending A/B.
- **No browser at any project of his.** An automated launch writes six fleet
  shapes per launch into the room. The `orphan-probe` fixture is where browser
  work belongs.

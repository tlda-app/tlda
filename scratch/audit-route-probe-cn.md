# Audit rows — S8 (route-probe-cn's slice)

Window: 64 commits on `main`, `--since='2026-08-17T19:00:00-04:00'` (commit date, `%cd`), no `--until`.
Repo: /Users/skip/work/tlda. Merged from 4 sub-audits (batches aa/ab/ac/ad, 16 commits each).

**Instrument note — read before trusting any row below.** The shared checkout sits on
branch `fix-bot-launcher-name-race`, not `main`; a bare `git grep <literal>` answers about
the wrong tree (measured: `refusedRevision` bare=0, main=8 — a false INERT on exactly the
pattern this audit hunts). **Every row in this file was produced or re-verified against an
explicit `main` reference** (`git grep <literal> main -- <pathspec>`, `git show main:<path>`,
or a dedicated worktree pinned to `main` tip). Batch ab's first pass ran bare and was fully
discarded and redone; its round-1 output does not appear here. `git log -S <literal> --all`
was bounded to `main`/a pathspec throughout, not run unbounded (times out past ~2min on this
box; `timeout(1)` does not exist here).

**Tier: all 64 of these commits are tier-1 (S8). 39 of the 64 are additionally tier-2**
(implementer work under chief `fleet:0a554e63`, fired after 10h) — marked `[T2]` below and
held to the higher bar per pm-audit's instruction.

## Tally

| verdict | count |
|---|---|
| KEEP | 50 |
| INERT(live) | 3 |
| CUT | 3 |
| UNESTABLISHED | 8 |
| **total** | **64** |

## Batch aa (16 commits)

| sha | subject | author | verdict | evidence |
|---|---|---|---|---|
| cfc1cdb43 [T2] | Accept a proposed commit iff it fast-forwards | actual-versioning | INERT(live) | Adds `POST /:name/source-bundle` in `server/routes/projects.mjs`. `git grep -n "source-bundle" main` → 2 sites: `server/routes/projects.mjs:2023` (the route) and `bin/a-checkout-proposes-a-commit-test.mjs:81` (its own test) — no daemon/client caller anywhere in the tree. Test crosses the real HTTP boundary and passes (`node bin/a-checkout-proposes-a-commit-test.mjs` → "passed"), so the receiver works, but nothing in production calls it. Commit message itself: "NOTHING CALLS THIS YET AND THAT IS THE POINT," declared deploy-1-of-3. **Checked whether the later deploys arrived rather than trusting the declared intent:** `git log --oneline main -S 'source-bundle' --since='2026-08-17'` → exactly 1 commit, `cfc1cdb43` itself. Nothing has wired it since. So this is INERT(live) with the staging claim unfulfilled, not merely "deliberately staged" — an intention, verified not (yet) a fact. |
| 6f6a18117 [T2] | Say that the new accept path has no post-accept effects yet | actual-versioning | INERT(live) | Rides on cfc1cdb43. Adds `postAcceptEffects: 'none-yet'` to the same unused response. `git grep -n "postAcceptEffects" main` → 1 hit, the field itself, no consumer. |
| 2729fba99 | Start a supervised bot as the bot it already is | bot-launcher-race | KEEP | Implements AGENTS.md §"One bot of a model, for its whole life" verbatim (Skip 2026-08-18 14:21-14:27 EDT). `node bin/bot-launcher-mint-id-test.mjs` on `main` → pass (mint-id derivation, restart reuse, non-bot mints unaffected). Commit discloses honestly the daemon-wire half needs a live deploy to fully prove. |
| fe0484686 [T2] | Prove a refused push reaches the checkout, over the wire it crosses | edits-dont-reach-the-file | KEEP | Wire-crossing test for the canonical `refusedRevision` bug. `node --test server/lib/a-refused-push-reaches-the-checkout.test.mjs` on `main` → 1 pass, 0 fail (49.4s, real server + real `/ws/fleet-daemon` socket + real git checkout, asserts `git rev-parse refs/tlda/refused/HEAD` on the far side). Authorization inferred from the documented AGENTS.md standard, not a literal per-commit Skip quote. |
| 51f477b9f [T2] | Share one bot-heartbeat survey, so a bot's death has a witness | he-is-not-the-monitor | KEEP | `git grep -n "bot-heartbeats" main` → 2 in-repo sites (module + re-export). Consumer is out-of-repo: `grep -n surveyBotHeartbeats ~/work/tlda-bots/todd/todd.mjs` → imported line 28, called line 319, in the bot's working copy (which IS the deployment per standing guidance). Wire real across both repos. |
| 48abaff67 [T2] | Sweep activity search entries past 30 days, and only those | fleet-db-blocking | KEEP | `_maybePruneActivityEventsFts` defined `server/lib/fleet-store.mjs:2120`, called from `insertEventRecord:2050` — real call site. `node --test server/lib/activity-fts-prune.test.mjs` on `main` → 5/5 pass, including a deliberately-broken-delete probe. No literal Skip quote found authorizing this specific sweep; inferred from the standing "nothing rotates or sweeps anything" problem class. |
| 945d7df89 [T2] | Attach a mint's task in the same operation that mints it | mint-attaches-its-task | KEEP (established, short of full real-daemon proof) | Root-cause fix with numbers: 15s MCP durable-send deadline < ~20s real login time, dropping the second `delegate` leg on every mint since 42fe1daa2 (2026-08-08). **Strengthened following a live-seat outage tonight, without touching the live seat path:** (1) `node --test mcp-server/mint-delegate-queued.test.mjs` → 3/3 pass, using a stubbed transport (no real daemon/login) — proves `spawn` now carries the `delegate` body in one durable call, `await_ready` is gone, and a `queued` response preserves the payload for retry instead of dropping it. (2) Traced (not just read) the real control flow in `server/unified-server.mjs`: `performSpawnRelay` calls and `await`s `performDelegate(...)` at `:2698` — durably attaching the task — strictly *before* `sendDaemonDurable(...)` at `:2744`, the call that asks the daemon to launch anything. Not a DB transaction (no crash-atomicity guarantee), but there is no longer a second network leg to abandon — the original race is structurally gone, not relocated. (3) On causation for tonight's "gave up requesting a fleet seat" failures: `git grep -n "gave up requesting a fleet seat" main` → both hits live in `daemon/mint-core.mjs:205` (the seat-registration retry loop) and its test — a different file/subsystem from what this commit touches. Doesn't prove this commit innocent of everything, but is real evidence tonight's specific failure string comes from an unrelated path. Gap that remains: no live-wire proof of the full mint→login→attach sequence under real timing — that needs the real-daemon test, out of bounds tonight. No mint/spawn/daemon RPC was executed to get any of this. |
| ae248d55e [T2] | Forward refusedRevision across the mirror RPC | actual-versioning | KEEP | The fix for the canonical `refusedRevision` bug. `git grep -n refusedRevision main` → 30 hits, both ends live (`server/lib/shadow-mirror-rpc.mjs:66` sends, `daemon/shadow-mirror.mjs:28,62,69` receives and writes `refs/tlda/refused/HEAD`). Cross-checked against fe0484686's passing wire test. |
| b13ed14c8 [T2] | Make a thread read mean the conversation, not a slice of one participant | fleet-db-blocking | KEEP | Real measured bug: pair-filter applied in JS after the store's LIMIT. Commit cites live-box measurement (`thread(agent: app-lockup, page_size: 5)` → 0 of 4 before, 4 of 4 after). `git grep -n _queryPairEventsForSearch main` → defined `server/lib/fleet-store.mjs:5722`, called `:5980` — real call site. |
| 2e58f9583 [T2] | Surface a refused push where its author can see it, and make the spec current | actual-versioning | KEEP | Server: `markRefused` (`server/lib/source-git-store.mjs:341`) → `moveRef`. Daemon: `daemon/shadow-mirror.mjs:69` fetches into `refs/tlda/refused/HEAD`. `git grep -n "refs/tlda/refused" main` → 17 hits, production + docs + the already-verified passing wire test. |
| 9c5b74942 [T2] | Tell an agent macros did not load instead of that it has no preamble | msgid-reference | KEEP | Real bug: `Object.keys(macros).length` collapsed 9 failure modes into one wrong message. `node --test test/chat-render-check-macros-unknown.test.mjs` on `main` → 6/6 pass. Authorization weaker: commit attributes the fix to "its own words for what it wanted" — the reporting agent's stated want, not a dated Skip quote. |
| 7390a7ba5 [T2] | Stop one message pinning the head of the daemon's delivery queue | msgid-reference | KEEP | Skip quoted directly, 2026-08-18: "why are activity cards fucking brokeb." Root cause measured live on `mini:testing` (1,496 pending, 22-min queue). `outboxInflightDeadlineSeconds` real and daemon-configured (`shared/config.mjs:600`, `shared/daemon-config-schema.mjs:28`). `node --test daemon/outbox-inflight-deadline.test.mjs` on `main` → 5/5 pass. |
| d89d20bb8 [T2] | Run one mirror at a time per project | actual-versioning | KEEP (no dedicated test) | Cites a real, timestamped production error from Skip's own paper (`cannot preserve bregman@394f283...` at 09:24:51Z). Diff adds `serializeProjectMirror`, a correct-on-read per-project promise-chain queue. Ships no automated test — repro not checked in — so this rests on diagnosis + mechanism read, not a re-run proof. |
| 53718a35b [T2] | A failed mirror is not a failed build | actual-versioning | **CUT** | Removes `'mirror'` from the `phases` array `projectRevisionStatus` checks. `node --test server/lib/build-revision-wire.test.mjs` on `main` → **1 of 5 fails** (asserts `status==='building'` for a pending mirror, got `'success'`). Counterfactual: same test passes at `53718a35b~1` unchanged. This commit's own diff breaks a pre-existing test it never touched, despite claiming "verified across every phase combination." Design intent may be right; shipped state is red. |
| 17b0d9c6a [T2] | Say why the doc-version reload guard is not a missing null check | msgid-reference | KEEP (docs-only) | Pure 22-line JSDoc addition, `git show` confirms zero code lines changed. Zero behavioral risk. |
| 220b83b7f [T2] | Assert the whole mirror payload, not the fields someone remembered | actual-versioning | KEEP | Companion to ae248d55e — tightens `bin/shadow-mirror-rpc-adapter-test.mjs` to assert the whole payload. `node bin/shadow-mirror-rpc-adapter-test.mjs` on `main` → "shadow mirror fan-out: ok". |

## Batch ab (16 commits)

| sha | subject | author | verdict | evidence |
|---|---|---|---|---|
| c16e8472a [T2] | Delete the build-era mirror, and its switch with it | actual-versioning | KEEP | **Manager flag re-checked directly:** `mirrorPaused` claimed a live dead lever. `git grep -n "mirrorPaused" main -- .` → 2 hits, both prose comments explaining the removal (`docs/source-authority-state-machine.md:78`, `server/lib/build-runner.mjs:1690`) — no schema field, no writer, no route. `git grep -n "mirror-paused" main -- .` → 0 hits (route fully gone). Complete revert of both route and field, not an orphan. Authorization: Skip → bhief-of-staff, 2026-08-17 04:34:28-32 EDT: "we are supposed to mirror changes back to git... it's not a precondition, that's backward" (`thread`, verified). |
| 901055fa0 [T2] | Say what the mirror does to a dirty checkout | actual-versioning | KEEP | Test-only (+66), same mirror-on-accept path. Same authorization citation (04:34:28 EDT). |
| 67a109593 [T2] | Mirror on accept, so a push commits the author's checkout | actual-versioning | KEEP | `git grep -n "acceptedRevisionMirrorHandler" main` → 4 hits: declared, set, checked, called (`server/routes/projects.mjs:926,929,981,991`) — real chain. `git grep -n "markMirrored" main` → 5 hits, full chain both ends. Same authorization citation. |
| 6001197b4 [T2] | Make a source revision a commit | actual-versioning | KEEP, with a confirmed defect | Production wiring real (`server/routes/projects.mjs:1294-1295` awaits correctly). **But** changed `bootstrap()`/`submit()` sync→async without updating 3 test call sites; ran all 3 on a `main`-tip worktree — all crash (`AssertionError, actual: undefined`). Any row citing those 3 tests as green is void (none in this file do). Authorization: Skip → chief fleet:0a554e63, 2026-08-18 03:23-24 EDT: "GET ME A FUCKING ACTUAL VERSIONING SYSTEM... MIRRORING TOMORROW MORNING" (`search`, verified). |
| f2253daac [T2] | Allow the source wire harness its daemon socket | actual-versioning | KEEP (trivial) | Single allowlist entry, `bin/websocket-boundary-guard.mjs` (+7/-2), no product code. |
| b9d4302ae [T2] | Stop the spec and its tests asserting the bug as a requirement | edits-dont-reach-the-file | **UNESTABLISHED** | Wire: doc + 3 test-assertion files, no runtime literal — spec/test correctness change. Searched `search()` for a matching Skip quote, none found. Real change, ask untraced. |
| a896569ec [T2] | Reproduce, over the wire, the accept that deleted his prose | edits-dont-reach-the-file | **UNESTABLISHED** | `git grep -n "durable-source-wire-harness" main` → 6 hits, genuinely shared by 4 real tests. The "deleted his prose" incident this title claims was searched for directly — no matching hit found; a related-but-distinct 06:04-06 EDT exchange doesn't name deleted prose specifically enough to cite. |
| 5296f6867 [T2] | Discard a build that finishes after a newer one | msgid-reference | KEEP | `readPublishedAcceptSeq`/`isSupersededByPublished` inserted directly ahead of the DVI publish call (`git show`, confirmed not just defined). Unit test `build-publish-ordering.test.mjs` (+88). Skip quoted directly, 2026-08-18: "you just dont use a earlier build even if it ends later." Self-labeled "NOT PROVEN" for the full race by its own author; guard itself is real and live. |
| b9593f59f | Reference the stale-base evidence instead of copying it into the journal | app-lockup | **UNESTABLISHED** | `evidenceReference`/`hydrateOperation` real, called at 6 sites, exercised on every read path. No Skip quote found; not searched to exhaustion. |
| 5838959dc [T2] | Drop the settle deadline to 300s now the 14 GB copy is gone | edits-dont-reach-the-file | KEEP (trivial) | Single config constant (+18/-12), stated numeric justification, reversible, no new wire. |
| 0ce9b6722 [T2] | Push the edit he made just before the daemon restarted | edits-dont-reach-the-file | **UNESTABLISHED** | `detectUnpushedLocalEdits` real, called once on the restart-recovery path. Searched for the implied incident — no matching Skip message found. |
| ec83ccdaa [T2] | Stop a thread read from materializing an agent's whole history | fleet-db-blocking | **UNESTABLISHED** | `_queryAgentEventsForSearch` real (`server/lib/fleet-store.mjs:5638`, called `:5989`), index present. Commit's own measured number (39,450ms→34ms) is self-verification, not Skip's ask. Searched, no hit. |
| b6161bc45 [T2] | Show the last good render while a rebuild runs | msgid-reference | **UNESTABLISHED** | `git grep -n "buildStatus === 'building'" main -- src/App.tsx` → 1 hit, live. Commit's own author flags a follow-on as unproven; searched for the implied incident, no hit found either. |
| d57aafabb [T2] | Allow the message-id wire test its socket | msgid-reference | KEEP (trivial) | `git grep -n "event-by-id" main` → 12 hits, sender + receiver + 3 tests, real. This commit itself is a 6-line test-allowlist addition. |
| eb9a5cb47 [T2] | Say which checkout each daemon actually runs | sync-manifest-reconcile | KEEP (docs) | `docs/current-main-architecture.md` only (+14/-3), confirmed present on `main`. No runtime literal, no wire risk. |
| 9b5bed9f1 [T2] | Say which way the snapshot and the manifest differed | bhief-of-staff | **UNESTABLISHED** | Real literals `undelivered`/`undeclared` (`git grep`, corrected from a wrong first-pass guess), 2+ hits, asserted verbatim by their own test. Motivating incident ("bregman repeated this exact cycle four times in 2.5 hours") searched for, no matching Skip quote found. |

## Batch ac (16 commits)

| sha | subject | author | verdict | evidence |
|---|---|---|---|---|
| ed300a58c [T2] | Read a message back by the id we hand out | msgid-reference | KEEP | Sender `mcp-server/fleet-tools.mjs:4608`, receiver `server/unified-server.mjs:6914`, both on `main`. `git log -S "event-by-id" --all --oneline` shows introduced here, not dropped/re-added. Dedicated real-socket wire test. Skip, `search`, 8/17 11:25-26 PM EDT: "apparently you can't ook uo chat messages given id... you hvae to be able to fucking look them up." |
| 27e3ccd04 [T2] | Make unchecking Agent subtitles stop the one on screen | msgid-reference | KEEP | Verified against all 4 routes in `docs/settings-controls.md` (re-checked on request): checkbox writes `radio-subtitles-enabled`, read at `src/voice.mjs:698,4141`, `collapseRadioSubtitle` a real function not a stub, no per-shape prop shadow, no CSS-var route. Full round trip live on `main`. Bug quoted verbatim in commit: "i unchecked gthe fucking radio thing and the subs didnt stop." |
| 8960db602 [T2] | Chip a bare message id in chat again | msgid-reference | KEEP | Checked against settings-controls.md — not actually a pref control (no `preferences.ts` key), it's inline chat-render behavior; verified end-to-end anyway. Restores code deleted by 40267f183; `CANONICAL_REFERENCE_SOURCE` import + both restored use-sites present and consumed downstream (`FleetChatShape.tsx:4010,4161,6064,6113`), not dead. Regression restoration, not new scope. |
| 218d50751 [T2] | Apply the journal storage shape to every record, not just the new one | app-lockup | KEEP | `journalStorageShape` defined `:384`, called at the sole write site `:399`. Fixes the measured bregman `operations.json` 12.2MB/41-record incident (same thread as ee508ca17/9b5dcaafe). |
| 2787cc075 [T2] | Write down the init-filter narrowing readShadowIndexInfo introduced | app-lockup | KEEP | Comment-only (`git show` — every added line is `//`), 0 behavior changed. |
| 006036217 [T2] | Stop reading a whole git log to compute two numbers | app-lockup | KEEP | `rev-list HEAD` replacing full-log walk, `server/lib/shadow-changelog.mjs:156`. Equivalence checked against all 758 live shadow repos, 0 mismatches; 986.8ms→28.8ms measured. |
| ee508ca17 [T2] | Stop storing a second copy of pushed file bytes in the operation journal | app-lockup | KEEP | `withoutFileContent` defined `:417`, called `:389,862`. Measured before/after on real store (10.07MB→26.2KB after 20 pushes; parse 52ms→1.55ms); tied to 218/599 lag-profiler stalls on this read path. |
| c9bfa258e [T2] | Give an unanswered source push a deadline, and stop the two lies beside it | edits-dont-reach-the-file | KEEP | `expireStalePending` real (`daemon/source-sync.mjs:141`, called on a tick `:895`), config threaded through daemon config schema. Mechanism for the bregman-held-3-hours incident confirmed live in Skip's 8/17 late-night thread. |
| 9b5dcaafe [T2] | Stop copying the version history into every push | sync-manifest-reconcile | KEEP | Narrowed snapshot (`LIFECYCLE_MUTABLE_FILES`) confirmed on `main` at HEAD (`server/lib/project-store.mjs`). Dedicated cost test. Same live incident thread: 14GB `.source-lifecycle` against a 33MB source tree. |
| c504ae687 | Delete a file the server still holds after the disk lost it | bhief-of-staff | KEEP | `queueServerHeldButMissing` defined `daemon/source-sync.mjs:1057`, called at 2 sites. Wire-crossing test drives real `createSourceSync`, asserts on the emitted `source-change`. Resolves the `.bak-before-deletion.tex` saga; Skip, 8/17 ~11:20-27 PM EDT: "DELETE IT" / "FUCKING HANDLE A PROBLEM FOR ONCE." |
| e9c3ba890 | Revert "Make LaTeX membership the closure of the document's roots" | bhief-of-staff | **UNESTABLISHED — escalate** | The reverted commit `22fb6182` has a verified Skip approval (8/17 4:33 AM EDT, "the transitive closure of the document roots... that's it"). The **revert** itself carries zero body text and no traced authorization. Falls inside the same night's `.bak-before-deletion.tex` firefight (contradictory "DELETE IT / DONT DELETE IT / STOP ASKING ME SHIT"); c504ae687 (44 min later) fixes the actual root cause a different way. Reads as unilateral incident triage, not a Skip order. **Recommend pm-audit/chief-night pull the full `bhief-of-staff <> skip` thread 17:00–23:59 on 8/17 to settle whether he ordered this.** |
| 879173af6 | Read the pill's editing line off the events the client already has | bhief-of-staff | KEEP | Old mechanism (`source-activity-payload.mjs`, `GET /:name/source-activity`) fully gone from `main`; new `src/pills/source-activity-from-events.ts` present, imported by `BuildProgressPill.tsx`. Commit quotes Skip directly ("push to the pill like we push to chat") — quote not independently re-verified against thread this pass, flagged medium-confidence. |
| 10c88c75c | Make the no-git fallback yield the same files git yields | bhief-of-staff | KEEP | Explicit before/after comparison in commit body: 1,066 files each way via fallback vs `git ls-files`, 0 mismatches. No Skip quote traced; defensible as fixing the guard 3d8c484f8 introduced. |
| 3d8c484f8 | Let the websocket guard run without a git checkout | bhief-of-staff | KEEP | Fallback present at HEAD (`bin/websocket-boundary-guard.mjs` ~357-394). Verified by commit's own before/after (copied tree without `.git`, guard exits 0). No Skip quote traced. |
| 8ebe85e52 | Split the fast guards from the ten-minute ratchet | bhief-of-staff | KEEP | `lint:guards` script present at HEAD, exact 8-guard chain named in diff. Mechanical, low-risk. No Skip quote traced. |
| a6b83ead3 | Clear the two guards blocking the lint gate | bhief-of-staff | KEEP | Both fixed guards (websocket allowlist, source-conflict-delivery-test) confirmed present/correct at HEAD. `npm run lint` red since 2026-08-12 is a real, checkable pre-existing condition. No Skip quote traced for this specific work. |

*(879173af6, 10c88c75c, 3d8c484f8, 8ebe85e52, a6b83ead3 — wire/mechanism prong solid for all five; authorization prong is commit-message-only or absent. Flagging per default-cut-when-unproven if a stricter bar is wanted on the authorization prong specifically; graded KEEP here on "real fix for a genuinely broken/red gate," not on a traced instruction.)*

## Batch ad (16 commits, window 2026-08-17 19:00–21:09 EDT — no tier-2 overlap)

| sha | subject | author | verdict | evidence |
|---|---|---|---|---|
| f92dfdef7 | An unreadable recording is not the same as no recording | bhief-of-staff | INERT(live) | Server marks `{unreadable:true,...}` but `RecordingSummary` type has no such field and the only consumer (`src/overlays/RecordingsButton.tsx:55-56`) unconditionally renders `r.title`/`fmtDur(r.duration_ms)` for every item — garbled blank row, not the distinguishable state claimed. |
| 1946a553b | Stream recording audio, and honour Range | bhief-of-staff | KEEP | `node bin/recording-audio-stream-test.mjs` → 6/6 ok. Real HTTP server, asserts 206/416/Range bytes. |
| f8719ca7e | Age a pending replica from the revision, not from its last attempt | bhief-of-staff | KEEP | Store + prune script both switched to `lifecycle.acceptedAt`, consistent. Own test currently red on `main` — but caused by 6001197b4 (see below), not this commit; patched locally, 7/7 pass. Production caller already correct. |
| 93ebca482 | Add a one-off prune for lifecycle journals already on disk | bhief-of-staff | KEEP | `bin/prune-source-lifecycle-journals.mjs` mirrors write-path rules exactly; dry-run default, backs up before `--apply`. Addresses measured 130,251,627 bytes / 179 journals. |
| b25dc9bf8 | Prune the daemon outbox at-most-once ledger | bhief-of-staff | KEEP | `node bin/daemon-outbox-ledger-prune-test.mjs` → 4/4 ok. Addresses measured 6,376,523 rows / 621MB, no prior DELETE anywhere. |
| d0c2aac49 | Expire a replica that has been pending too long, and drop its payload | bhief-of-staff | KEEP | Same 6001197b4-caused test regression as f8719ca7e; patched locally, 7/7 pass incl. 2 new expiry checks. Production correct. |
| b6e9ad313 | Only sweep the trace store when something is over a bound | bhief-of-staff | KEEP | `node bin/control-plane-trace-trim-test.mjs` → 6/6 ok. |
| 3193e0c36 | Delete the empty subscription-wake function and its unreachable consumer | bhief-of-staff | KEEP | `grep -rn` → 0 code hits, only comment references. Deleted function body was empty; its tests deleted correctly since the target no longer exists. |
| 18e18c4e5 | Drop a replica's command payload once it is no longer pending | bhief-of-staff | KEEP | Origin of the replica-command chain. Same 6001197b4-caused regression (landed 8.5h before it, so not its fault); patched locally, original 5 checks pass. |
| a66977440 | Name the instrument that answers without measuring | bhief-of-staff | KEEP | AGENTS.md-only, self-reported incident writeup, no wire to verify. |
| b99fb1267 | Name the second route to a wire that reports health | review-routes-mcp | KEEP | AGENTS.md-only, documents the finding b2934d999/ef7085929 were built to fix. |
| 02716242a | Delete the sync-IO guard: it was blind, and the instrument already existed | bhief-of-staff | KEEP | `grep -rn "installSyncIoGuard\|sync-io-guard"` → 0 hits anywhere. Matches measured named-import blindness finding (29/35 server modules import by name). |
| b2934d999 | Prove a real edit reaches the pill, over the wire | review-routes-mcp | **CUT** | Test file doesn't exist on `main` HEAD. Whole `signal:source-activity` mechanism it proved was deleted 3h later by 879173af6, per Skip's own words quoted there ("push to the pill like we push to chat"). Already reverted; work not in the product. |
| ef7085929 | Tell the pill when a source file's editors change, don't ask every second | review-routes-mcp | **CUT** | Implementation half of the above; `server/lib/source-activity-payload.mjs` does not exist on `main` HEAD (`git show main:<path>` fails). Fully removed same night by 879173af6. |
| 9eaade708 | Name the line number as the everyday case of an undated claim | bhief-of-staff | KEEP | AGENTS.md-only, self-contained. |
| 7e614cfe0 | Cite the route call sites by commit, not by line number | bhief-of-staff | KEEP | Comment-only edit, matches diff exactly. |

## Cross-batch defect found during this audit (not a keep/cut row itself)

**6001197b4** [T2] (batch ab, KEEP-with-defect above) changed `bootstrap()`/`submit()` sync→async
without updating 3 test call sites — `bin/replica-command-not-retained-test.mjs`,
`bin/a-figure-does-not-refuse-a-rebase-test.mjs`, `test/linked-remote-divergence.test.mjs` —
all crash on `main` right now (`AssertionError, actual: undefined`). Production call sites
(`server/routes/projects.mjs:1294-1295`) already await correctly, so this is not a live
production bug, but it silently voided the test-based proof for 3 other real fixes in this
slice (18e18c4e5, d0c2aac49, f8719ca7e — all graded KEEP above on patched-locally re-runs,
not on the currently-red tests as shipped).

## UNESTABLISHED (8 of 64 — not mixed into KEEP/CUT/INERT above, listed again here per spec)

b9d4302ae, a896569ec, b9593f59f, 0ce9b6722, ec83ccdaa, b6161bc45, 9b5bed9f1, e9c3ba890
(e9c3ba890 flagged for escalation — see its row).

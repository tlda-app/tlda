# tlda recovery chief: complete non-marketplace work list

Status carried forward at **2026-09-01 02:56 EDT**.

## Authority and boundary

This list recovers the operation begun by Skip's 2026-08-31 21:03:55 message, “theres a lot of shit that's still fuxked about 1. classroom 2. sync,” through his 2026-09-01 01:36 handoff request to mint a new SOL chief for “literally everything you kicked off 5 hours ago.” The source is the complete paginated Skip ↔ prior-chief chat thread:

- `thread(filter: "skip <> fleet:add50b89", types: ["chat"], since: "2026-09-01T01:00:00Z", until: "2026-09-01T02:12:22Z")`
- continuation through 2026-09-01 01:35:06 EDT, then `since: "2026-09-01T05:35:06.720Z"` through the 01:36 handoff.

The 15-minute p3m/OpenVSX 0.2.7 propagation monitor remained with `/root` and was outside this operation. `/root` completed it at 2026-09-01 03:04:26 EDT: the exact Positron p3m Gallery feed independently returned `tlda-classroom / tlda-labs / tlda Classroom / 0.2.7`. This list carries that established dependency result without duplicating its monitoring work.

## Current baseline

| Surface | Exact state | Disposition |
|---|---|---|
| Local `main` | Scratch/report-only commits on top of code baseline `c0af90c1116968b1328144b140b95fda80b7145e`; no later application-code commit | The code baseline contains the classroom UI, identity, layers, sync admission repair, HTML loader fixes, visual-mode callout CSS, and tlda Classroom 0.2.7 source. The checkout is dirty with unrelated search/subscription work; every owner must preserve it. |
| Air checkout and remote `main` | Both `c0af90c1116968b1328144b140b95fda80b7145e`, checked from Air | Current remote code baseline; the recovery-record commit is local and unpushed. |
| `tlda-pic-dev` | Fly machine started and healthy; `/api/build-info` inside the machine reports clean `main` at `6f112d6b568cf68cafb2657423fc561fdc52f252`, built 2026-09-01 03:34:52Z | Live, but deployed by the rejected direct-`fly deploy` route. It is diagnostic state, not accepted release proof. |
| Production PIC | Healthy, clean `main` at `7776bc9643b0792e5e4cc0e0f78d72effea6df26` | Older than current main. Production release is not authorized by this operation. |
| Public course site | HW −1 row/page and ZIP resolve. Current ZIP SHA-256: `4cb4a219dce11f0cb2244186583ff065281a5d3dc14b087f0851d3bc5b144a67`; four student files; answer block present and solution absent. Exact Positron p3m Gallery feed now serves tlda Classroom 0.2.7. | The content/store dependency is present; the exact student-facing search/install and end-to-end assignment chain still require the real Positron gate below. |

## Release, build, and publication process

Skip's controlling messages are 23:39:35, “but we should still use push to deploy”; 23:40:18, “organized; scripted; documented w/ process/culture docs”; 23:40:33–55, no unnecessary dependency on a giant book build and a chapter must be changeable minutes before class; and 23:41:57, process repair immediately after the shareable handout.

| Work item | Status and artifact | Owner | Next concrete action |
|---|---|---|---|
| Canonical pic-dev push-to-deploy | **Exact `274cb03e66ea68e9c8f7c96004cf14b7cfbfa23a` blocked after two independent reviews.** It supersedes blocked `e3907b4a0` and actual successor `27258864fac6e99f83292692dbf1e09f136f7ea4`. Its test passes unrelated-dirt canonical-main deployment, exact clean build-info, wrong-SHA refusal, hook-failure refusal/ref preservation, and hook-tamper detection. It does not exercise the distinct required case of canonical main pushed to a wrong ref. Nothing was installed, pushed, or deployed. | `classroom-repair-sol`, `fleet:9fae-mti8rdmb` | Add the actual shared-hook wrong-ref counterfactual and preserve all passing deploy evidence; return one amended exact SHA. |
| Fast classroom chapter update | **Exact `274cb03e6` blocked after review despite real-path timing.** The supported worker path passes at roughly 48–52s full and 28–35s changed-chapter through `materializeBuildInstance → buildQmdDocument → publishBuildInstance`, preserving changed/untouched pages, links, assets, and manifest. Two scope/proof defects remain: seeding happens before format selection and therefore changes HTML/Markdown/LaTeX behavior too; and the test never reads `toc.json` or an actual rendered TOC/sidebar entry. | `classroom-repair-sol`, same artifact | Restrict prior-output seeding to the requested QMD path with non-QMD negative controls, and assert the actual generated/user-visible TOC survives the partial update. |
| Existing `course-release` subsystem | **Deletion-first disposition passed exact review; branch remains held for other failures.** `274cb03e6` deletes the generic release core/CLI/tests/docs: 1,136 deletions versus 656 insertions across the whole artifact. Review confirmed no dangling references; its audit disposes every generic artifact kind, pointer, source-revision graph, manifest, retry, rollback, runner, and overlay. The public PIC edge runtime is retained separately and not relied on by this workflow. | `classroom-repair-sol` | Keep this deletion fixed while correcting the remaining release gates. |
| Mandatory student solution stripping | **Blocked on a demonstrated bypass.** The command rejects solution markers and checks answer-block count, but it never compares student QMD bytes with `stripSolutions(master)`. A ZIP containing the secret solution text inside one `.callout-answer` block passes its current gate. The existing red test injects the untouched master and misses this bypass. | `classroom-repair-sol` | Make publication derive or verify the actual stripped student bytes, and prove the exact solution-text-in-answer-block counterfactual goes red. |
| Atomic Pages page + download publication | **Blocked: current proof is synthetic.** The command only commits and pushes. Its test invents a custom bare-repo `post-receive` publisher with `releases/$sha` plus an atomic `current` symlink, so the mid-publication HTTP read proves that test mechanism, not the supported ordinary GitHub Pages path. | `classroom-repair-sol` | Establish the real GitHub Pages publication invariant or replace the supported path with a real mechanism that guarantees page/ZIP agreement; test that actual boundary. No course push. |
| Immediate Mini recovery | **Complete.** The fleet audit showed the older fleet already hibernating and the new team active. The chief stopped two stuck `bhief-4` restart process trees that had run for 4h46m. The owner stopped the three-hour-old held-highlight Vite/server tree. A fresh process audit found no Quarto preview, stale Vite, stale browser automation, or stuck restart process; only the current sync-proof server and parent-owned p3m Positron remained. | Recovery chief, with `sync-repair-sol` for its owned listeners | Keep current task resources bounded; the future script-owned cleanup row prevents recurrence. |
| Future script-owned Mini cleanup | **Passed exact review; branch remains held for other failures.** The retained production commands start no browser, Quarto preview, local server, or persistent child. The Pages regression closes its HTTP server in `finally`; real worker regressions assert worker exit on success and failure. A rejected synthetic unused cleanup helper was deleted. Fresh review found no branch-owned process/listener after the gates. | `classroom-repair-sol` | Keep this ownership boundary and cleanup proof fixed in the amended artifact. |
| Process/culture documentation | **Present but not accepted while its supported Pages/QMD claims fail.** `docs/live-deploy.md`, `docs/course-pages-publish.md`, and `docs/classroom-release-process-audit.md` name the pic-dev push path, separate course page/ZIP path, timing, cleanup boundary, and deletion disposition. The Pages and TOC claims currently exceed the proved mechanism. | `classroom-repair-sol` | Correct the implementation/proof first, then make the docs state only that accepted path. |
| Causal account of the day's delay | **Passed exact review; branch remains held for other failures.** The durable account separates build/deploy process, brittle code, and team/process causes; includes direct-Fly configuration, whole-tree rendering, HTML entering the SVG loader (`751cd10f0`), nested `_book/site_libs` handling (`6f112d6b5`), and the premature release/QA handoff, with honest unknowns and a prevention for each. | `classroom-repair-sol` | Preserve this exact causal account while correcting the remaining release gates. |

## Classroom app, book, and instructor surface

Skip's scope is the 21:14:04 whole book/TOC/appropriate Settings/no Project request, the 21:20–21:22 classroom layer and movable/resizable HUD discussion, the 21:05 mic/PIC-icon backlog, and the 22:12–22:13 locked classroom identity with preferred name and pronouns. His exact course identity value later was `skip (he/him)`.

| Work item | Status and artifact | Owner | Next concrete action |
|---|---|---|---|
| Full multi-member book, TOC, links, and assets | **Source fixes on main; cold user-visible proof held.** `751cd10f0` prevents HTML documents from entering the SVG page loader. `6f112d6b5` preserves nested `_book/.../site_libs` paths. | `classroom-ui-sol`, `fleet:bc8d-mti8rot0`, after exact push-deployed SHA | On a disposable book, prove every member, TOC entry, link, and asset on desktop and narrow layouts. No active course document. |
| Classroom chrome | **Implemented on main; proof held.** `fd204860c` keeps Settings reachable and removes Project in classroom mode. An earlier proposal for a broad classroom Settings allow-list was not grounded and was dropped. | `classroom-ui-sol` | Cold-check no Project tab and the normal appropriate Settings surface, plus a non-classroom negative control. |
| Instructor/student layers | **Implemented on main; proof held.** `c9978e0f9` composes all active student layers for the instructor while keeping Class writable and student layers read-only. The earlier single-layer path made the layer UI effectively invisible. | `classroom-ui-sol` | Prove instructor composition, student visibility boundaries, and Class write behavior on disposable accounts. |
| Classroom identity | **Implemented on main; proof held.** `4a01a1545` adds course/student preferred name and optional separate pronouns, locks classroom identity to server records, and removes the classroom Settings chooser while preserving ordinary-app editing. Corrections for student update, clear semantics, CLI help, and fixtures are included. | `classroom-ui-sol` | Initialize fresh disposable course/student records and prove locked identity in classroom plus editable identity outside classroom. Do not use `name=` links. |
| Instructor entry/bookmark/home-screen app | **Exact production route established; complete device proof pending.** At 22:04, `classroom-repair-sol` returned the existing production RW instructor URL with no `name=` and proved `/api/classroom/me` returns instructor authority plus the instructor-only gradebook surface. The unredacted capability remains in that private owner thread and is intentionally not committed here. Current production identity was browser-persisted; the newer server-owned `skip (he/him)` behavior is on the later classroom identity implementation and still needs the final accepted deployment proof. | `classroom-ui-sol` | Verify one no-`name=` URL as the instructor entry, stable authority/identity across reopening, and correct title/icon/manifest for Mac bookmark and iPad/phone home-screen installation. Use disposable browser/device state; state any genuinely physical-device-only missing proof rather than asking Skip to QA. |
| Mic control | **Implemented on main; proof held.** `fd204860c`: OFF is slashed, ON bare, equal opacity, unique mask, pointer activation. | `classroom-ui-sol` | Exercise both states with mouse/touch and confirm no glowing-red or opacity-only state. |
| PIC class-app icon | **Implemented on main; proof held.** `fd204860c` uses the real PIC mark in the classroom manifest; prior source test recorded SHA `323ad80…`. | `classroom-ui-sol` | Inspect the actual installed/home-screen/classroom surface and manifest bytes on exact push-deployed pic-dev. |
| Playback PiP/HUD | **Implemented on main; proof held.** `fd204860c` provides a movable/resizable pointer-and-touch playback HUD with cleanup tests. | `classroom-ui-sol` | Exercise move/resize/playback at desktop and narrow sizes; inspect console and cleanup. |
| LiveKit | **Settled: leave it in and unchanged.** The 21:14:30 deletion suggestion was superseded by Skip at 21:19:40: “lets actually leave livekit in.” `fd204860c` left it byte-identical. | `classroom-ui-sol` | Include a non-regression check only. Do not reopen deletion or redesign. |
| Final classroom headed gate | **Blocked on accepted release path.** Earlier checks stopped first on HTML/SVG loading, then nested `site_libs`; both source fixes landed. The gate was then correctly held because pic-dev was direct-Fly rather than push-deployed. | `classroom-ui-sol`; independent gate `classroom-delivery-advocate-sol`, `fleet:7d4f-mti8sjn1` | Resume only on the exact push-deployed pic-dev SHA and stop on the first real user-visible failure. |

## tlda Classroom extension and HW −1 student chain

| Work item | Status and artifact | Owner | Next concrete action |
|---|---|---|---|
| Extension identity and description | **Source on main and exact Positron feed at 0.2.7.** Correct display name is **tlda Classroom**; it is a general classroom/homework extension, not PIC-specific and not “tlda Homework.” `c0af90c11` is the 0.2.7 source commit; `/root` independently verified the exact p3m Gallery feed tuple at 03:04:26 EDT. | `classroom-ui-sol` verifies the actual installed surface | Search/install from the exact Gallery feed in real Positron and inspect manifest/README/commands. No class-specific claims or implementation-note prose. |
| Exact 0.2.7 artifact in real Positron | **Installed and connected on the disposable path; downstream blocked.** A fresh isolated Positron profile installed `/Users/skip/work/tlda/scratch/tlda-classroom-0.2.7.vsix` as `tlda-labs.tlda-classroom@0.2.7`; Quarto 1.136.0 was installed separately. Supported `tlda --env pic-dev classroom setup` created disposable course `mendel-verify-178824`, linked three projects, and froze handout `7e86cbf1e7ffd7facdf4c6d9a6db6a14d1ba0c45`. The real URI handler passed its native trust prompt and reported “Positron is connected to the class.” | `classroom-ui-sol` | Preserve this exact local profile as evidence while repairing the first student-source failure on a fresh disposable course. Do not use qtm285 or a fixture. |
| Registration/connect and student docs | **Both source defects committed and independently passed; fresh real chain running.** Disposable registration and connection succeeded, and the first source project failed because final rendering rewrote `_quarto.yml` to include the generated handout while `renderHomeworkVariants` omitted that QMD from the copied snapshot. Exact `fbc36a61b7ed03407a2c28ec94cf3cc052a7e974` adds the generated handout to the copied set and passed independent red ENOENT / green daemon-linked-file and solution-stripping review. Fresh `mendel-verify-179002` then rendered the solution because `publishClassroomAssignment` selected the solution-bearing master. Exact `98bca20c095af75785f8f0275906c4c2e63582b5` changes only the student source project's `mainFile` and `documentRoots` to `rendered.handoutSource`; separate handout and solutions projects are unchanged. Independent diff review passed, and the chief reran the focused command test 2/2, exit 0. Nothing is pushed or deployed. Original missing-file screenshot: `.playwright-cli/mendel-disposable-source-build-blocker.png`; exposed-solution screenshot: `.playwright-cli/mendel-179002-student-source-exposes-solution.png`. | `classroom-ui-sol` | Run a third fresh disposable course with exact `98bca20c0`; first prove source bytes and rendered route exclude solution content, then resume real Positron Preview/Check/Zip/Submit/Mine/instructor receipt and stop at the first failure. |
| Visual-mode callouts | **Source on main; real proof pending.** `ddd1b17f9` styles Quarto callouts. The older conclusion that CSP made this impossible, and the older proposal to change an answer title, are stale/superseded by this implementation. | `classroom-ui-sol` | In the exact installed 0.2.7 extension, inspect every supported Quarto callout type in Visual mode and Preview. Preserve screenshots/evidence; no Photoshop or fixture substitute. |
| Preview wording/behavior | **Required; proof pending.** The student-facing instruction is “Click Preview and make sure your homework looks like you intended it to.” “Render button,” command-palette detours, and “read what comes out” were rejected. | `classroom-ui-sol` | Click the actual Preview button in Positron and verify the generated homework. |
| Check, Zip, Submit | **Commands exist; full real chain unproved.** Older 0.2.4/0.2.6 package/socket/harness checks are historical and cannot close the 0.2.7 task. Dragging a file in Finder is not a substitute. | `classroom-ui-sol` | Run the actual commands in fresh Positron against the disposable classroom. Verify accepted image formats and the explained HEIC handling, metadata cleanup such as `__MACOSX`, archive bytes, and direct submission. |
| Mine and instructor receipt | **Unproved for 0.2.7.** | `classroom-ui-sol` | Prove the submitted archive appears intact in Mine and the disposable instructor receipt surface. |
| Public assignment + marketplace dependency | **Dependency resolved; real install gate pending.** The page instructs extension search rather than a direct VSIX. `/root` completed the exact-feed monitor: Positron p3m Gallery serves tlda Classroom 0.2.7 as of 03:04:26 EDT. | `classroom-ui-sol`; independent delivery advocate | Re-run the student-facing search/install in real Positron and continue the assignment chain; do not treat the feed query alone as installed-surface proof. |
| Historical 0.2.6 gate | **Superseded evidence, retained for audit.** The prior advocate found an old Positron download URL 404, class-specific README/description, and a held pic-dev chain. Later edits addressed the URL/description in 0.2.7 source, but only the exact 0.2.7 installed surface can replace the old failure. | `classroom-delivery-advocate-sol` | Compare the installed 0.2.7 artifact to each old failure; do not carry a stale PASS or stale BLOCK forward without rechecking. |

## Sync reliability and explanation

Skip asked at 21:10:06–45 for explanations, tests, and a causal account because it was hard to understand why sync still failed.

| Work item | Status and artifact | Owner | Next concrete action |
|---|---|---|---|
| Reproduced admission race | **Fixed on main; live proof and independent review complete.** Original local `1fb445512`, integrated as `5ba381d71`, records source admission before the worker can drain it. On exact current code, a disposable linked-checkout edit served in 40.3s; a deliberate 1ms observer deadline went red; the immediate retry served in 36.1s; a final cycle served in 51.5s. Queue/lifecycle rows were singular and complete; targeted ordering test passed 1/1. The advocate confirmed the report does not misattribute the unknown 17:43 incident to this race. | `sync-repair-sol`, closed task `fleet:b15c-mti8rzwk` | Preserve the verified path; no further sync code change is supported by this evidence. |
| 17:43 stale-build incident | **Cause remains unknown.** Viewer served `8a07ee7`; `4efdc41` completed about seven minutes later before the assigned agent intervened. There was no recorded manual repair. Logs needed for exact cause had rolled off. | `sync-repair-sol` | Write the causal explanation without attributing `5ba381d71` to this historical incident. State what the record proves, what it rules out, and what is unknowable. |
| Current observability/lifecycle | **Audited; no new subsystem added.** Existing project/status, source-lifecycle, and build-queue records expose accepted revision, acceptance order, queue admission/start/terminal disposition, build result, and final head. They do not persist watcher settle or the external served-view observation. The live wire passed, so the owner did not add duplicate observability. | `sync-repair-sol` | Preserve this restraint unless a reproduced failure establishes a missing first node. |
| Durable sync proof and tests | **Complete and independently passed after correction.** Fleet message `3526342` records the model, current-main live wire, red counterfactual, recovery convergence, exact queue/lifecycle evidence, cleanup, and the 1/1 regression. Message `3527505` corrects the stale-test accounting: all 3/3 `server/lib/source-lifecycle.test.mjs` tests fail immediately because they call removed operation-journal `prepareOperation` APIs, not the current admission-before-worker path. The isolated server was stopped; port 5267 has no listener. | `sync-repair-sol` | Keep the three stale lifecycle tests explicitly dispositioned; do not widen this passed live-wire repair into restoring removed journal APIs. |

## Source references and programmatic text highlighting

| Work item | Status and artifact | Owner | Next concrete action |
|---|---|---|---|
| Programmatic “show me where” highlight | **Exact `8b31ee7253d222c3789bc9fc546542c15030eca6` blocked after independent review.** Skip's 21:54–21:59 correction says the use case is the current document—“you said you put this in—where is it.” The commit matches rendered TeX text while preserving raw offsets, fingerprints SyncTeX cache files, and passes 9/9 unit/static checks. Its actual original-resolution evidence still overshoots the requested phrase: S7 continues past “headline” through “, and it is”; theorem continues past “twice” into “in the”; nearest continues past “The benchmark” through “is the later”. This matches the earlier real-route finding that one-word generated-line `x` records cross word boundaries; the synthetic geometry test assumes exact endpoints and cannot go red on that failure. The headed run also left two console errors unidentified, and its claimed ordinary-user negative control is only a source-regex assertion rather than an actual highlight interaction. Nothing is pushed or deployed. | `sync-repair-sol`, task `fleet:b15c-mti8s9vk` | Repair the real word-boundary geometry with red-before/green-after screenshots, preserve and disposition console output, and add an integrated ordinary-user-highlight negative control; return one amended exact SHA. |
| Rejected highlight commit | **Still blocked as a standalone artifact.** `3e3504e43` supplied the base route but its screenshots did not cover the exact full phrase; raw matching crossed TeX/token boundaries and proportional geometry overshot glyphs. It may only travel as the reviewed base of `8b31ee725`, never alone. | Same owner | Do not integrate or report `3e3504e43` separately. |
| Commit-range flashing as primary UI | **Rejected/superseded.** The earlier proposal to flash a commit/edit range is not the current “show me where” interaction. | No owner | Keep it out of the highlight repair. |
| `filename:lineno` chat linkification | **Open; queued separately and held at the highlight gate.** Skip's 21:49 request was not superseded by the later highlighting correction. | `sync-repair-sol`, after highlighting, `fleet:b15c-mti8yn95` | After independent highlight PASS, recognize file-qualified source references and navigate to the exact source file/line with red-before/green-after tests. |
| Bare `line x` / line-range semantics | **Open; queued with source-reference work.** Intended meaning is the currently viewed source document when unambiguous. | Same queued task | Implement current-file semantics with safe refusal for ambiguous/no-current/stale/non-reference cases. Do not substitute clicking for programmatic highlighting. |

## Settled corrections that must not regress

| Rejected or superseded route | Current direction |
|---|---|
| Deploy classroom work to general `testing` | Classroom development/release target is pic-dev. |
| Direct `fly deploy` as accepted pic-dev workflow | Use canonical push-to-deploy. The current direct-Fly deployment is diagnostic only. |
| Treat a dirty working tree as evidence that committed `main` cannot be deployed | Skip explicitly rejected this at 23:38:31–38. A push contains the committed canonical `refs/heads/main`; unrelated uncommitted bytes are not part of it and must not block the push. |
| Delete LiveKit | Leave LiveKit in and unchanged. |
| Broad classroom Settings allow-list | No such product decision was made. Keep appropriate normal Settings, with only the separately requested classroom identity chooser locked/removed. |
| Clickable link or commit-range flash as the primary “show changed text” interaction | Use programmatic highlighting in the current document. Keep file/line linkification as a separate usability feature. |
| Treat the public HW −1 as having become correct by a later 0.2.6 marketplace publication | Historical audit established 0.2.6 was published before that handout was built. The problem was handoff/QA claims, not later luck. |
| Use old 0.2.4/0.2.6 socket, harness, or source checks as proof of the current extension | Exercise exact 0.2.7 in real Positron and across the real disposable classroom boundary. |
| Use qtm285 or any active user course for QA | Use a fresh disposable course/book/student. |
| Require a mechanism that literally performs no rebuilding | Measure the current path and meet the actual fast-update/no-giant-dependency requirement without forcing architecture. |

## Ownership and immediate sequence

1. `classroom-repair-sol` owns the release/build/publication/cleanup lane and must return an independently review-ready local path before any pic-dev mutation.
2. `classroom-ui-sol` owns the exact local 0.2.7 Positron chain now, using a fresh disposable course. Its classroom headed gate remains held until the accepted push-deployed SHA exists.
3. `sync-repair-sol` completed the sync explanation/proof. Its first frozen programmatic-highlight repair failed independent review on real phrase geometry, console evidence, and the ordinary-user negative control; file/line source-reference work remains held until an amended highlight artifact passes.
4. `classroom-delivery-advocate-sol` independently gates the list, release path, real Positron chain, and final push-deployed classroom surface. It does not implement or contact Skip.
5. `/root` completed the separate 15-minute p3m 0.2.7 propagation monitor at 03:04:26 EDT; the exact Gallery feed now serves 0.2.7. No owner above duplicates that completed monitoring work.

No production/stable/testing deployment, marketplace publication, active user document, or message to Skip is authorized by this handoff.

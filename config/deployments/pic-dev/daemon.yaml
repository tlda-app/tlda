# Daemon/environment authority for an isolated course-development deployment.
machineId: fly

environments:
  default: pic-dev
  values:
    pic-dev:
      database: https://course-dev.example.invalid
      store: https://course-dev.example.invalid
      licenseKey: ""

taskDoc:
  globalDir: /app/server/persist/fleet-task-doc

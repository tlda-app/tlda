# fly.test-overleaf.toml — throwaway test box for a document-import proof.
# Separate Fly app (`your-tlda-import-test`), not the live application.
# Unlike fly.live.toml this one:
#   - serves the SPA itself (public http_service) instead of tailnet-only
#   - is token-gated (TLDA_TOKEN_RW/READ secrets), not TLDA_NO_AUTH
#   - auto-stops when idle (scale-to-zero compute; only the small volume bills)
# Deploy:  fly deploy -c fly.test-overleaf.toml
app = "your-tlda-import-test"
primary_region = "sjc"

[build]
  dockerfile = "Dockerfile.live"

[env]
  PORT = "5176"
  NODE_ENV = "production"
  # Everything else this server needs now lives in config/deployments/overleaf-test/,
  # committed and baked into the image. TLDA_DEPLOYMENT names that directory and
  # is the only pointer that must arrive from outside it.
  TLDA_DEPLOYMENT = "overleaf-test"

[http_service]
  internal_port = 5176
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0

[[vm]]
  size = "performance-1x"
  memory = "4096"

[mounts]
  source = "test_overleaf_data"
  destination = "/app/server/persist"
